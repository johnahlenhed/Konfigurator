// NOTE: CG delivered these as 'emptyUp'/'emptyDown', not the 'socket'-prefixed
// naming convention originally requested (socketAddon1/socketAddon2).
// Decided to work with their actual names rather than request another
// export round. Mapping confirmed via position: emptyUp (y=+0.6) = top-right
// slot, emptyDown (y=-0.6) = bottom-right slot.

export const SOCKET_ADDON_1 = "emptyUp";
export const SOCKET_ADDON_2 = "emptyDown";